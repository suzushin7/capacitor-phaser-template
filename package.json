{
  "name": "capacitor-phaser-template",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "npx vite build && npx cap copy",
    "build:android": "npx vite build && npx cap copy && npx cap open android",
    "build:ios": "npx vite build && npx cap copy && npx cap open ios",
    "sync": "npx cap sync",
    "preview": "npx vite preview",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "@capacitor/android": "^7.2.0",
    "@capacitor/cli": "^7.2.0",
    "@capacitor/core": "^7.2.0",
    "@capacitor/ios": "^7.2.0",
    "@capacitor/screen-orientation": "^7.0.1",
    "@capacitor/status-bar": "^7.0.1",
    "phaser": "^3.88.2",
    "vite-plugin-javascript-obfuscator": "^3.1.0"
  },
  "devDependencies": {
    "@types/node": "^22.14.0",
    "typescript": "^5.8.3",
    "vite": "^6.2.6"
  }
}
