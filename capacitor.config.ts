import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.suzushinlab.capacitorphasertemplate',
  appName: 'capacitor-phaser-template',
  webDir: 'dist'
};

export default config;
